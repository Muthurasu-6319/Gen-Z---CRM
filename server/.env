DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=crm_db
JWT_SECRET=crm_super_secret_jwt_key_2024
PORT=3001
UPLOAD_DIR=uploads

# SMTP Email Configuration (for Mailbox send feature)
# Fill in your SMTP credentials to enable actual email sending.
# If left blank, emails are logged to the DB but not dispatched.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
